{
  "name": "spectacular-monorepo",
  "version": "0.0.0",
  "description": "Monorepo for Spectacular service and CLI",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "format": "biome check . --write",
    "lint": "pnpm --recursive lint",
    "typecheck": "pnpm --recursive typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.8.2"
  }
}
